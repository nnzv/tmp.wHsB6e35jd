name: "9967c0fee1af75594662"
on:
  workflow_dispatch: {}
concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true
jobs:
  1-group:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 10s
